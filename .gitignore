# ═══════════════════════════════════════════════════════════════════
#  SalamTV — ما لا يدخل المستودع أبداً
# ═══════════════════════════════════════════════════════════════════
#  المستودع عام (وهو إلزام GPL-3، لا اختيار). فكل ما هنا سرّ يفقد
#  قيمته لحظة رفعه — ومفتاح التوقيع خاصّةً: من يملكه يستطيع نشر
#  تحديث موقَّع باسمك يُثبَّت على أجهزة مشتركيك بلا سؤال.
# ═══════════════════════════════════════════════════════════════════

# ── مفاتيح التوقيع — الأخطر على الإطلاق ──
*.jks
*.keystore
*.p12
*.pem
*.pepk
signing*.properties
keystore*.properties
owntv-signing.properties

# ── أسرار البيئة ──
.env
.env.*
!.env.example
secrets.properties
google-services.json

# ── مسارات محلية (تختلف بين الأجهزة) ──
local.properties
.gradle/
gradle.properties

# ── مخرجات البناء ──
build/
*/build/
*.apk
*.aab
*.aar
captures/
.cxx/

# ── بيئات التطوير ──
.idea/
*.iml
.DS_Store
Thumbs.db

# ── تحقيقات محلية ──
*.hprof
*.log

# ── من قالب GitHub لأندرويد ──
.externalNativeBuild/
output-metadata.json
misc.xml
deploymentTargetDropDown.xml
render.experimental.xml
